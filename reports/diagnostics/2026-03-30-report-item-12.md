## Item
Missing signal references preventing auto-close functionality for multiple trades (IDs: 282, 199, 189, 187, 186, 185, 184, 183, 181, 180)

## Signals
- 10 trades with `signal_id: null` failing auto-close checks
- Warning pattern: "Signal None not found for trade XXX, skipping auto-close"
- All affected trades appear to be older positions (trade IDs in 180-282 range)
- 2 additional 404 errors fetching prices for weather trades (196, 191)

## Likely Causes
1. **Database integrity issue**: Signal records deleted or orphaned after trades were opened
2. **Migration problem**: Recent database schema change didn't properly migrate signal_id references
3. **Signal cleanup job**: Automated cleanup may have removed signals while trades remained open
4. **Trade type mismatch**: These trades may have been created without signals (manual entries or legacy format)

## Next Checks
1. Query database: `SELECT id, signal_id, trade_type, opened_at FROM trades WHERE id IN (282,199,189,187,186,185,184,183,181,180)`
2. Verify signal table: `SELECT COUNT(*) FROM signals WHERE id IN (SELECT signal_id FROM trades WHERE signal_id IS NOT NULL)`
3. Check deletion logs for any signal cleanup jobs run recently
4. Review trade creation logic to confirm signal_id is properly set

## Recommendation
**Immediate**: Disable auto-close for trades with null signal_id to prevent error spam
**Short-term**: Audit all open trades and either reconnect signals or mark for manual management
**Long-term**: Add foreign key constraint to prevent signal deletion while trades are open
