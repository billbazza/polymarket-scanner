## Item
50 open trades vs 34 closed - position management/exit strategy imbalance

## Signals
- **Open/Closed Ratio**: 1.47:1 (50 open vs 34 closed) - too many positions staying open
- **Trade Types**: Mix of weather (7 open), copy trades (43 open from wallet 0xed107a85...)
- **Open Trade Ages**: Oldest weather trade from 3/30 19:56 UTC (signal #199)
- **Missing Signals**: Warnings show "Signal None not found" for trades 180-184
- **Copy Trade Concentration**: 43/50 open trades from single wallet, all BUY_NO positions at low prices (0.0055-0.0345)

## Likely Causes
1. **Copy trading dominance**: Single wallet (0xed107a85...) generating bulk of open positions with no exit logic
2. **Missing signal references**: Trade-signal linkage broken (signal_id=null for copy trades)
3. **Weather trades not resolving**: 7 weather positions open despite approaching/passed target dates
4. **No exit strategy for copy trades**: All copy trades lack exit criteria or stop-loss logic

## Next Checks
```sql
-- Check copy trade performance
SELECT copy_wallet, COUNT(*) as trades, 
       SUM(CASE WHEN status='open' THEN 1 ELSE 0 END) as open,
       AVG(entry_price_a) as avg_entry
FROM trades WHERE trade_type='copy' 
GROUP BY copy_wallet;

-- Check weather signal resolution status
SELECT ws.id, ws.target_date, t.status, t.opened_at
FROM trades t 
JOIN weather_signals ws ON t.weather_signal_id = ws.id
WHERE t.status = 'open';

-- Check for stuck auto-close processes
SELECT * FROM trades 
WHERE status='open' AND trade_type='weather' 
AND opened_at < NOW() - INTERVAL '24 hours';
```

## Recommendation
1. **Immediate**: Pause copy trading from wallet 0xed107a85
