# 2026-03-30 Bug Fix Log

## Source
- Manual QA notes from [Bugs_log.md](/Users/will/Obsidian-Vaults/polymarket-scanner/Bugs_log.md)

## Fixes Applied
- Cointegration `Trade` now refreshes Open Trades immediately after opening a paper trade.
- Cointegration `Brain` now handles Anthropic model failures more safely through centralized response normalization and model fallback logic.
- Whale `Brain` now uses the structured whale validator path and no longer crashes when whale alerts contain missing numeric fields such as `current_price`.
- Whale `Trade` now handles alerts with `current_price = NULL` by falling back to a safe default entry price instead of failing and returning `None`.
- Whale and cointegration Brain/trade UI handlers now surface backend error details rather than generic failures.
- Added persistent UI preferences for relevant checkbox controls.
- Added a persistent `Hide sports` filter for the cointegration tab.

## Files Changed
- [brain.py](/Users/will/Obsidian-Vaults/polymarket-scanner/brain.py)
- [server.py](/Users/will/Obsidian-Vaults/polymarket-scanner/server.py)
- [whale_detector.py](/Users/will/Obsidian-Vaults/polymarket-scanner/whale_detector.py)
- [dashboard.html](/Users/will/Obsidian-Vaults/polymarket-scanner/dashboard.html)

## Verification
- `python3 -m py_compile` passed for patched Python files.
- Dashboard JavaScript passed `node --check`.
- Live `POST /api/brain/whale/722` returned `200 OK` after restart.
- Live `POST /api/brain/validate/3056` returned `200 OK` after restart.
- Whale trade construction was dry-run against alert `722` and produced a valid `BUY_NO` payload with fallback entry price `0.5`.

## Maintenance
- When future bug fixes are made, add a new dated file under `fix_logs/`.
- `CLAUDE.md` and `GEMINI.md` both require updates if the bug-fix logging process changes.
